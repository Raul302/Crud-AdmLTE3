// import { Navigate } from "react-router-dom";

// function PrivateRouter({ children }) {
//   const auth = true;
//   return auth ? <>{children}</> : <Navigate to="/login" />;
// }


// export default PrivateRouter; // Make sure to export it as default